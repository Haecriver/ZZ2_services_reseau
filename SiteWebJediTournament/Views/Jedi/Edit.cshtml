@model SiteWebJediTournament.Models.JediModels

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>JediModels</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.Nom, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Nom)
                @Html.ValidationMessageFor(model => model.Nom)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IsSith, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.IsSith)
                @Html.ValidationMessageFor(model => model.IsSith)
            </div>
        </div>

        <h5 @Html.DisplayNameFor(model => model.Caracts)></h5>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Caracts.Default.Def)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Caracts.Default.Nom)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Caracts.Default.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Caracts.Default.Valeur)
                </th>
            </tr>

            @foreach (var item in Model.Caracts.List)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Def)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nom)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Valeur)
                    </td>
                </tr>
            }

        </table>

        <div class="form-group">
            @Html.LabelFor(model => model.Caracts, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select name="caractselect" id="caractselect">
                    @Html.Action("ListCaracteristiqueJedi", "Caracteristique")
                </select>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
